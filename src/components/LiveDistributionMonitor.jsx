export default function LiveDistributionMonitor({ data }) {
  const pending = !data
  return <section className="monitor" id="live-distributions" aria-labelledby="monitor-title"><div className="section-heading"><h2 id="monitor-title">Live Distribution Monitor</h2><span className="status-pending">Live monitor setup pending</span></div><div className="monitor-stats">{['Total tracked distributed','Unique recipients','Verified transfers','Last detected'].map((label)=><article key={label}><span>{label}</span><strong>{pending ? '—' : data[label]}</strong></article>)}</div><div className="latest-box"><div><h3>Latest distribution</h3><p>{pending ? 'Monitoring data will appear here after a backend worker and database are connected.' : ''}</p></div><div className="evidence-table monitor-table"><div className="table-row table-head"><span>Recipient</span><span>Amount</span><span>Time</span><span>Evidence</span></div><div className="empty-row">No live monitor data connected yet.</div></div></div></section>
}
